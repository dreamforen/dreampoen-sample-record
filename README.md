# 드림포이엔 시료채취기록지 웹 v5

- 먼지 / 중금속 기록지를 완전히 독립 상태로 분리
- 24시간제 시간 입력(HH:MM)
- 원형 굴뚝: 단면적 기준 1/4/8/12/16/20점 자동 산정
- 원형 측정점 위치: 등단면적 반경식 적용, 관벽 2.5 cm 최소 이격 반영
- 사각형 굴뚝: 상등직경 계산, 면적·종횡비 기반 격자 자동 분할
- 계산된 측정점 수만큼 위치표 및 입자상 측정조건 입력행 자동 생성
- 표준산소 입력 시 보정 전/후 유량 자동 표시
- 보정 전/후 농도 입력란 제거
- 팀별 오리피스계수 및 노즐 선택, K-factor·오리피스압차 자동계산
- 적산유량계 전/후 및 누출검사
- 가스상 측정조건 항목 선택 추가

GitHub Pages에서는 index.html / style.css / app.js / README.md 4개 파일을 기존 저장소에 덮어쓰면 됩니다.


## v7 변경사항
- 원형/사각형 전체 법정 측정점과 대표 측정구 입력 지점을 분리
- 원형: 대표 측정구는 반경 구분수에 따라 1~5지점만 표시 (예: D=0.78 m → 0.276 m)
- 사각형: 전체 격자수는 내부 산정, 대표 측정구는 가로 방향 중심거리 1~5지점만 표시
- 담당자 서명 박스 제거, 이름 옆 `(인)` 표시
- 먼지 시료채취시간 입력란을 제목 바로 옆으로 정렬


## v10 변경사항
- 원형 대표 측정점 로직 유지
- 사각형 대표 측정구 위치를 격자 중심 좌표로 변경 (예: 0.80×0.80 m, 2×2 등분 → 1지점 0.200×0.200 m)
- 원형/사각형 측정점을 SVG 단면 그림에 자동 표시


## v10 사각형 측정점 보정
- 단면적별 최대 L(0.5 / 0.667 / 1.0 m) 적용
- 가로·세로 각각 ceil(길이/L)로 최소 분할 수 산정, 0.25 m² 초과 시 최소 4등분
- 전체 격자와 현장 대표 측정구의 실제 입력 지점을 분리
- 0.80 × 0.80 m 입력 시 2×2 등분, 대표 1지점 0.200 × 0.200 m
- 계산 결과를 사각형/원형 도식에 자동 표시


## v10 측정구 표시
- 원형/사각형 모두 오른쪽 측부를 측정구로 고정
- 단면 그림 오른쪽에 측정구와 프로브 삽입방향 자동 표시
- 사각형 측정점 그림은 오른쪽 측정구 기준으로 좌우 방향을 맞춰 표시
- 기존 계산값 및 원형/사각형 측정점 산정 로직은 유지


## v11 산출근거(로우데이터) 인쇄
- 기록지 뒤에 산출근거 페이지 추가
- 수분량, N2, 표준/실제 밀도, 유속, Vic, 노즐면적, 굴뚝면적, 오리피스압차, 등속흡입계수, Qa, 산소보정 Q 표시
- 각 항목의 기호, 계산식, 대입값, 산출값, 단위를 함께 출력
- 지점별 원시 측정값 표 추가
- 인쇄/PDF 시 산출근거 페이지는 새 페이지에서 자동 시작
- 먼지/중금속 전환 시 각각의 독립 입력값으로 재계산


## v12 장비 선택 개선
- 1팀 노즐: 0.317 / 0.472 / 0.609 / 0.759 / 0.957 / 1.088 / 1.263 cm
- 2팀 노즐: 0.312 / 0.450 / 0.533 / 0.612 / 0.777 / 0.938 / 1.094 / 1.267 cm
- 노즐 기타 직접입력 추가
- 기타 입력 시 preset 버튼 선택 자동 해제, 계산에는 직접입력값 사용
- K-factor 옆에 등속흡인계수 즉시 표시
- 장비 선택 영역 압축 및 작은 모니터용 반응형 배치 개선


## v13 노즐 UI 수정
- 작은 모니터에서 기타 직접입력란이 노즐 버튼과 겹치던 문제 수정
- 노즐 버튼은 4열 고정 정렬
- 기타 노즐 직접입력은 별도 줄로 분리
- 매우 좁은 화면에서는 2열로 자동 전환


## v14 수정
- 오리피스계수를 측정 장비 패널에서 제거하고 피토관계수 바로 옆으로 이동
- 1팀/2팀 버튼 클릭 이벤트 누락 복구
- 팀 선택 시 오리피스계수와 노즐 목록이 즉시 변경되도록 정상화


## v15
- 피토관계수 옆 오리피스계수 칸을 표 형태로 정돈
- 계수값과 고정값 안내 문구가 겹치지 않도록 분리


## v16
- 누락된 원형/사각형 입력 전환 함수 복구
- 원형 선택 시 내경만 활성화, 가로/세로 자동 비움·비활성화
- 사각형 선택 시 가로/세로만 활성화, 내경 자동 비움·비활성화
- 단면 전환 즉시 측정점/시료채취 행 재계산


## v17 현장 기록관리
- 하루 여러 측정건을 개별 기록으로 저장
- 오늘 작성 기록 목록에서 클릭하여 다시 열기/수정
- 입력 중 0.7초 후 브라우저 자동저장
- 최초 수동저장 전에도 작성중 초안을 자동 보존하고 다음 접속 시 복구
- 기존 기록 수정 후 '기록 저장' 시 기존 건 업데이트
- 수동 저장 직전 확정본 1개 자동 백업 및 '최근 저장본 복구'
- '복사해서 새 기록'으로 업체/시설 조건을 재활용 가능
- 현재폼 초기화는 저장 기록을 삭제하지 않음
- 기록은 현재 사용하는 브라우저/기기 내부(localStorage)에 저장


## v18
- 새 기록 → 시설추가
- 복사해서 새 기록 버튼 삭제
- 시설추가 시 업체명/날짜/담당자/기상/장비정보 유지, 시설별 입력값 초기화
- 오늘 작성 기록: 접수번호 / 업체명 / 시설명 순
- Excel 출력: 인쇄 가능한 시료채취기록지 양식 + 산출근거(로우데이터) 시트
- A4 가로, 1페이지 너비 맞춤, 병합셀/테두리/구획 적용


## v19
- Excel 불러오기 추가: v18 이후 웹에서 출력한 시료채취기록지 Excel을 다시 업로드하여 자동복원
- 일반사항 제목 제거, 이후 섹션 번호 재정리
- 브라우저/PDF 및 Excel 인쇄방향 A4 세로로 변경
- 산출근거 수식을 분수형 표시로 개선해 분자/분모 구분
- 불러온 Excel은 웹에서 수정 후 기존 기록관리 기능으로 다시 저장 가능


## v20
- 첫 기본정보 카드 제목을 '현장측정정보'로 지정
- Excel 불러오기 엔진을 ExcelJS에서 SheetJS(XLSX)로 교체하여 출력본 재업로드 안정성 개선
- 시트 위치/행 위치가 조금 변해도 항목명과 헤더를 검색해 입자상/가스상 데이터를 복원
- 인쇄 레이아웃 최종 재정리: 현장측정정보~자동계산결과 1페이지, 산출근거 2페이지
- A4 세로, 첫 페이지는 약 58% 축소, 로우데이터는 큰 글씨로 별도 페이지 사용


## v21
- Excel 출력 첫 시트를 현재 웹 시료채취기록지 흐름에 맞춰 전면 재배치
- A4 세로 1페이지에 현장측정정보~자동계산결과가 들어가도록 FitToPage 적용
- 담당자(부) manager2를 Excel 출력에 추가
- 굴뚝/장비조건 아래 측정점 위치 표시 영역을 Excel에 포함
- 원형: 대표 측정점 거리, 사각형: 대표점 좌표 및 격자 표현
- 산출근거(로우데이터)는 별도 두 번째 시트/1페이지로 인쇄 설정


## v21.1 hotfix
- Excel 출력 버튼이 반응하지 않던 원인 수정
- 측정점 그림 영역에서 전체 셀 병합 후 내부 셀을 다시 병합해 ExcelJS가 오류를 내던 구조 제거
- Excel 출력 오류 발생 시 사용자에게 오류 메시지가 보이도록 예외 처리 추가


## v22
- 업로드된 기존 DFEN-QPF-14-01 인쇄양식을 기준으로 Excel 첫 시트 레이아웃 재구성
- 상단 접수번호/업체/시설/담당자/책임기술자 + 현장측정정보 + 입자상 + 가스상 순으로 A4 세로 1페이지
- 웹의 측정점 SVG를 PNG로 변환해 Excel에 그대로 이미지 삽입
- 원형/사각형/측정구/점 위치가 웹 화면과 동일한 그림으로 출력
- 가스상 측정조건은 사용자가 추가한 항목 수만큼 행 자동 증가
- 숨김 __WEB_DATA 시트에 전체 웹 입력값을 저장하여 Excel 재업로드 시 정확히 복원
- 산출근거는 별도 2번째 시트/1페이지 유지

## v23 인쇄/PDF 최종 레이아웃
- 일반 웹 화면을 축소 인쇄하지 않고 별도 인쇄 전용 문서 생성
- A4 세로, 상하좌우 10mm 여백
- 1페이지: 기존 DFEN-QPF-14-01 스타일의 현장측정정보 + 측정점 그림 + 입자상 + 가스상
- 측정점 그림은 현재 웹 SVG를 그대로 복제하여 원형/사각형/측정구/지점이 동일하게 출력
- 가스상 조건은 실제 추가한 항목 수만큼만 행 생성
- 2페이지: 수분량/밀도/유속/채취수분/등속흡입계수/유량/산소보정 계산을 큰 분수식과 변수값으로 배치
- Excel 기능은 유지하되 공식 출력물은 인쇄/PDF 중심

## v24 인쇄/PDF 개선
- 1페이지 강제 압축 제거
- A4 세로 기준 상하 10mm / 좌우 9mm 여백
- 표 전체를 인쇄 가능 가로폭 100%에 맞추고 세로는 자연스럽게 흐르도록 변경
- 제목을 표 밖 독립 중앙 제목으로 이동해 세로 깨짐 방지
- 현장측정정보 3열 비율 유지, 측정점 SVG 그대로 출력
- 입자상 표는 폭 우선으로 유지
- 가스상 조건은 추가된 행만 표시, 많으면 자연스럽게 다음 페이지로 넘어감
- 산출근거는 2페이지 이후 큰 글자/큰 수식으로 출력


## v25
- 2페이지 로우데이터는 v24 그대로 유지
- 1페이지 오른쪽 잘림 방지를 위해 A4 안전 여백을 좌우 11mm로 조정
- 상단/현장정보/입자상/가스상 각 표의 열 비율을 100% 이내로 명시
- 셀 패딩을 안전하게 줄이고 1페이지 글씨는 오히려 소폭 확대
- 측정점 그림은 화면 SVG를 단순 복제하지 않고 흰 배경 인쇄전용 SVG로 재생성
- 측정점 그림의 검은 사각형 출력 문제 보정


## v26 - 웹 입력화면 세로축 재설계
- 기능/계산/세팅값/팀별 장비값/노즐값/오리피스계수/K-factor/측정점 계산 로직 변경 없음
- 저장/기록관리/Excel 불러오기/가스상 추가/자동계산 로직 변경 없음
- 기존 HTML ID와 JavaScript 로직을 그대로 보존하고 CSS 배치만 변경
- 웹 본문 최대폭 980px의 A4 세로 문서형 화면으로 정렬
- 기존 가로 다단 카드 배치를 기본 1열 위→아래 흐름으로 변경
- 카드 내부의 짧은 입력항목만 2열 유지, 작은 화면에서는 1열
- 측정점 그림을 중앙 넓은 영역에 배치
- 2페이지 로우데이터 인쇄 레이아웃은 변경 없음


## v27
- 상단 헤더 폭과 본문 폭을 980px 동일 축으로 정렬
- 오늘 작성한 기록을 오른쪽 세로 박스가 아니라 전체폭 가로 영역으로 표시하도록 보정
- 입자상 측정조건은 가로 스크롤 없이 한 화면에서 보이도록 표 폭/글자/패딩 조정
- 인쇄/PDF는 별도 printDocument 양식을 사용하지 않고 현재 웹 화면 레이아웃을 그대로 출력하도록 변경
- A4 세로 인쇄 시 현재 화면 전체를 비율 축소하여 폭에 맞춤
- 계산/세팅값/팀 장비값/측정점/저장 데이터 로직은 변경하지 않음


## v28
- 상단 '대기 시료채취기록지' 헤더 폭을 본문 980px과 정확히 동일하게 맞춤
- 오늘 작성한 기록을 먼지/중금속 탭 오른쪽으로 이동, 기록카드를 가로로 스크롤/열람
- 피토관계수(C)와 오리피스계수를 동일 행의 강조 박스로 정리
- 입자상 12열 표의 열별 폭을 재배분하여 가로 스크롤 없이 전체 표시
- 산출근거는 No/기호/단위 폭을 줄이고 산출식/대입값 폭을 확보하여 한 화면 표시
- 지점별 원시 측정값 10열도 열별 폭 재배분하여 한 화면 표시
- 인쇄는 현재 화면 구조를 기준으로 A4 폭에 맞춰 출력
- 계산/저장/팀별 장비/측정점 로직은 변경하지 않음


## v29
- 오늘 작성한 기록을 기록저장/시설추가 등 액션 버튼 아래로 이동
- 기록 목록을 가로 카드가 아니라 세로 목록으로 변경
- A4 세로 인쇄에서 한 페이지 강제 없음
- 현재 웹 세로형 화면 구조를 그대로 유지하면서 A4 폭에 맞게 자연스럽게 여러 페이지 출력
- 표/입력칸/측정점 영역은 인쇄 시 자동 축소 및 폭 맞춤
- 계산/세팅/저장/측정점 로직은 변경하지 않음


## v30
- 오늘 작성한 기록을 액션버튼 바로 아래 왼쪽 영역(약 72%)에 세로목록으로 배치
- 인쇄 첫 페이지 상단에만 '드림포이엔 시료채취기록지' 제목 표시
- @page margin 0 + 내부 여백 방식으로 브라우저 날짜/시간/URL 머리글 공간 최소화
- 굴뚝·장비조건부터 인쇄 2페이지 강제 시작
- 프린트 시 선택된 흡인노즐 버튼만 진한 색으로 강조
- 오리피스압차 평균 / K-factor / 등속흡인계수를 한 줄 3칸으로 정렬
- 적산유량계 채취 전/후/차 및 누출검사 영역 확대
- 입자상 측정조건 / 먼지 시료채취시간을 왼쪽 정렬
- 계산/세팅/장비값/측정점 로직 변경 없음


## v31
- 프린트에서 오늘 작성한 기록/기록목록 완전 숨김
- 앞쪽 기록지 글자 크기 확대(라벨 약 9pt, 값 약 9.3pt)
- 검정선 위주의 팩스 느낌을 줄이고 연한 블루/그레이 문서형 디자인 적용
- 섹션 제목을 문서 헤더 스타일로 정리
- 굴뚝·장비조건은 2페이지 시작 유지
- 선택 노즐만 진한 색으로 명확하게 표시
- 자동계산 3칸과 적산유량계/누출검사 영역을 더 크게 정리
- 입자상/가스상 표 글씨 및 행높이 확대
- 로우데이터는 기존 만족 상태를 최대한 유지


## v32 - 기존 DFEN XLSM 불러오기
- 기존 회사 시료채취기록지 `.xlsm`의 숨김 `기록부` 시트를 원시 인자값 기준으로 인식
- 업체명 D4 / 시설명 E5 / 측정일 D6
- 피토관계수 S4 / 기온 S5 / 대기압 S6 / 측정위치대기압 V7
- 노즐직경 E8 / 굴뚝내경 T8 / 날씨 Y8 / 습도 Y9 / 풍속 Y10 / 풍향 Y11
- O2 E10 / CO2 D11 / 수분 E13 / 여지번호 R13 / 적산유량계 전 Y13
- 입자상 지점별 값은 기록부 18~22행의 온도/정압/동압/진공압/미터온도/임핀저/채취량/시간을 자동 복원
- 가스상은 기록부 31~47행 중 실제 측정값이 있는 행만 자동 추가
- 기존 웹 출력 Excel(__WEB_DATA)과 v18+ 출력형식 불러오기도 그대로 유지


## v33 - 기존 XLSM 양식 그대로 출력
- 사용자가 업로드한 기존 `기록지(먼지)` XLSM 파일을 `dreampoen_record_template.xlsm`로 그대로 포함
- Excel 출력 시 새 통합문서를 만들지 않고 템플릿 ZIP/XML의 값 셀만 수정
- 열너비/행높이/병합셀/색상/인쇄영역/매크로(vbaProject)를 변경하지 않음
- 숨김 `기록부` 원시값과 보이는 `기록지(먼지)` 값을 동시에 배치
- 출력형식 `.xlsm` 유지, Excel 재계산을 열 때 강제
- 기존 XLSM 업로드 불러오기 기능 유지
- 제목 번호: 1 현장측정정보 / 2 기상 / 3 산소·CO2 / 4 수분 / 5 굴뚝·장비 / 6 입자상 / 7 가스상 / 8 자동계산 / 9 산출근거
- 업체명/시설명 입력란을 전체 폭으로 확대


## v34 - 기존 XLSM 출력 안정화
- 숫자 셀에 NaN/undefined/null이 절대 기록되지 않도록 공백 처리
- 담당자/담당자(부)/책임기술자 이름에서 기존 `(인)` 문구를 제거 후 1회만 추가
- 산소량/CO2/등속흡인계수/노즐직경/굴뚝 치수 단위를 기존 양식 셀에 표시
- 기록지의 유속/유량 값은 라벨 셀이 아닌 실제 값 셀로 매핑 수정
- 적산유량계/누출검사 셀 매핑 수정
- 1~5지점 측정점 위치를 웹 산출결과에 맞춰 기존 기록지에 자동 입력
- 웹 측정점 SVG를 스타일 포함 PNG로 변환하여 기존 Excel 중앙 그림 위치에 삽입
- 등속계산 탭에 수분, 밀도, 유속, Vic, I factor, 유량 등 계산 가능한 인자값/산출값 입력
- workbook.xml을 재작성하지 않아 기존 VBA/매크로/통합문서 구조 손상을 최소화


## v35
- 측정점 그림 Excel 삽입 크기: 너비 8.7 cm × 높이 6.9 cm 고정
- 웹에서 출력한 XLSM의 숨김 `기록부!AZ200`에 전체 웹 기록 JSON을 함께 보존
- 따라서 출력한 XLSM을 다시 `Excel 불러오기` 했을 때 셀 몇 개만 재구성하지 않고 전체 상태를 우선 복원
- 담당자/담당자(부)/책임기술자, 전체·먼지 채취시간, 원형·사각형 선택 및 치수, 팀/노즐 선택, 수분/O2/CO2 배열, 1~5지점, 가스상 추가행 등 재업로드 정확도 개선
- 기존 회사 XLSM처럼 AZ200 상태가 없는 파일은 기존 `기록부` 셀 매핑 방식으로 계속 불러옴
- 가스상 legacy 재업로드 범위를 기록부 30~47행으로 보정


## v36
- Excel 측정점 그림 크기 8.7cm × 6.9cm 유지
- 기존 템플릿의 측정점 원형 도형 중심을 기준으로 새 PNG 위치 재조정
- 그림을 좌측/상단으로 이동하여 기존 측정점 틀 안에 들어가도록 배치
- 웹→XLSM→웹 재업로드, 셀 매핑, 등속계산 값, 팀/노즐/측정점 계산은 v35 그대로 유지


## v37
- 굴뚝내경 Excel 배치 분리: 원형은 기존 상단 내경칸, 사각형은 상단칸을 비우고 아래 2칸에 가로/세로 각각 입력
- 수분량 셀 및 출력 XML에서 `#REF!` 오류가 노출되지 않도록 정리
- 누출검사 결과는 템플릿의 기존 가운데정렬 스타일을 보존하도록 값만 입력
- v36의 측정점 그림 8.7 × 6.9cm 틀맞춤, 재업로드, 등속계산 등 기존 기능 유지


## v38
- 측정점 PNG를 고정 크기/오프셋이 아니라 기록지의 실제 셀 틀(J9~P22)에 고정
- Excel drawing anchor를 twoCellAnchor로 변경하여 틀의 좌상단/우하단 경계에 맞춤
- 재출력 시 이전 자동그림이 중복되지 않도록 기존 생성 그림 제거 후 1개만 삽입
- v37의 Excel 매핑 및 웹 재업로드 기능은 그대로 유지


## v39
- v38의 J9~P22 전체 셀범위 확대 배치를 제거
- 측정점 그림을 사용자가 확인한 기존 크기 8.7cm × 6.9cm로 복원
- Excel 중앙 측정점 사각형 위치에 고정 배치
- 나머지 v38 기능/계산/재업로드/등속계산은 변경 없음


## v40 - 사용자가 맞춘 Excel 그림 위치 그대로 적용
- 사용자가 업로드한 `20260826-12_주식회사 삼손종합정비_시료채취기록지 1.xlsm`의 실제 그림 앵커값을 직접 확인하여 적용
- 시작 위치: col 9 / colOff 23946 / row 8 / rowOff 341150
- 그림 크기: cx 3132000 / cy 2395122
- 즉, 더 이상 셀 범위나 추정 좌표로 맞추지 않고 사용자가 Excel에서 확인한 위치와 동일하게 삽입
- 계산, Excel 재업로드, 등속계산, 원형/사각형 및 기타 v39 기능은 변경하지 않음


## v41
- 사각형 굴뚝 치수 Excel 배치 수정
- 사각형일 때 G19=가로, I19=세로로 같은 줄에 나란히 출력
- 기존 세로값을 G20에 넣으면서 수분 흡습관법 값에 덮어써지던 문제 수정
- 원형은 기존 원형 내경 칸만 사용
- v40의 측정점 그림 위치 및 나머지 기능은 그대로 유지


## v42
- 굴뚝내경 사각형 배치 재수정
- 가로값: G19
- 세로값: H19 (기존 단위칸 I19에서 한 칸 왼쪽으로 이동)
- 단위 m: 위쪽 I18 / 아래쪽 I19에 각각 표시
- v40에서 확정한 측정점 그림 위치와 나머지 기능은 그대로 유지


## v43 - 통합관리 좌측 메뉴 1단계
- 기존 v42 시료채취기록지 기능/계산/Excel 출력 코드는 변경하지 않음
- 화면 왼쪽에 고정 사이드 메뉴 추가
- 메뉴 순서: 업체관리 → 일정관리 → 일정등록 → 시료채취기록지
- 현재 시료채취기록지는 기존 화면 그대로 표시
- 업체관리/일정관리/일정등록은 다음 단계 연결을 위한 빈 화면만 생성
- 인쇄 시 사이드 메뉴는 출력되지 않고 기존 시료채취기록지만 출력


## v44 - 업체관리 1차 완성
- 기존 공용출장일정 통합관리 v2.7의 `shared_db.json` 업체 343개를 웹 데이터로 이관
- 검색: 상호/주소/대표자/환경기술인/사업자번호/연락처
- 관리연도 및 월 선택
- v2.7과 동일한 월별 `1차 / 2차`, 상반기 측정일, 하반기 측정일, 연간관리상태 표시
- 측정주기 판정: 반기/연2회, 연1회, 분기/4회, 월/12회
- 미측정 상태 빨간 경고, 연간완료 녹색 표시
- 업체 상세정보 및 시설별 측정정보 조회
- 업체 기본정보 수정/신규 업체 추가
- 시설 추가/삭제, 시설별 측정주기 및 측정항목 관리
- 브라우저 수정 데이터는 localStorage에 보존, DB JSON 내보내기 지원
- 시료채취기록지 내부 코드는 변경하지 않음
- Excel 템플릿은 사용자가 새로 수정한 `dreampoen_record_template.xlsm`으로 교체


## v45 - 업체 기본정보 모달 수정
- 업체 선택 후 `기본정보 수정`을 눌렀을 때 빈 모달처럼 보이거나 닫기(X)가 동작하지 않던 문제 수정
- 모달 내부가 동적으로 다시 생성될 때 닫기/취소 이벤트가 끊기던 문제 해결
- X / 취소 / 모달 바깥 영역 클릭 모두 정상적으로 닫힘
- 업체관리 데이터 구조와 시료채취기록지 기능은 변경하지 않음


## v46 - 측정인 상·하반기 이력 통합
- 상반기측정일.xlsx 732건 + 하반기측정일.xlsx 186건, 총 918건 반영
- 사업자번호 → 주소 → 업체명 순으로 기존 업체와 매칭
- 기존 DB에 없던 업체 2개 자동 추가
- 기존 DB와 맞지 않던 측정시설 56개 시설별 측정정보에 추가
- 측정인 측정항목을 업체/시설 측정항목에 합쳐 누락 항목 보완
- 측정이력 918건을 업체 및 시설 이력으로 저장
- 업체관리 탭: `전체 / 1월 / 2월 ... / 12월` 추가
- 전체 탭은 업체별 12개월 측정일·시설수·측정건수 및 상·하반기/연간상태를 한 화면에서 확인
- 월 탭은 기존 1차/2차와 상·하반기 상태를 상세 표시
- 상세정보 하단에 측정인 측정번호/시설/항목/상태/기술인력 이력표 추가
- 연간관리상태는 시설별 측정주기와 실제 시설별 측정이력을 함께 확인하도록 개선
- 시료채취기록지 기능은 변경하지 않음


## v47 - 일정관리 달력
- 기존 `원본_출장일지_및_일정_26년.xlsx`의 2026 월별 일정 469행을 읽어 동일 날짜·내용의 담당자를 묶어 366개 일정으로 이관
- 기존 측정인 완료 이력과 날짜가 일치하는 연결업체 일정은 완료상태로 자동 표시
- 월 달력: 월~일 7열 × 6주, 하루 최대 4개 일정 + 추가건수 표시
- 상태 3단계: ○ 예정 / ● 확정 / ✓ 완료
- 일정관리 하단: 선택 날짜 전체 일정 목록
- 일정 확정/확정취소/측정완료/완료취소/삭제 기능
- `측정출장` 완료 시 연결된 업체의 MeasurementHistory 및 월별 1차/2차 Tracking을 자동 갱신
- 완료 취소 시 해당 일정으로 생성된 측정이력만 제거하고 월별 상태 재계산
- `이 날짜 일정 등록` 클릭 시 일정등록 메뉴로 이동하고 선택 날짜를 임시 저장
- 업체관리 v46 및 시료채취기록지 기능은 변경하지 않음


## v48
- 일정관리 재편성: 기존 측정출장 일정은 제거하고 상·하반기 측정인 실제 측정이력 기준으로 381개 일정 재생성
- 실제 측정자료 일정은 전부 `● 확정 + ✓ 완료` 처리
- 기존 연차/휴가 일정 20개는 그대로 유지
- 업체 매칭: 사업자번호 → 주소 → 업체명 순서로 강화, 미매칭 그룹 0개
- 시료채취기록지 Excel 출력 시 표준산소농도(stdO2) 저장 및 표시
- 등속계산 탭 원본 산소보정 수식이 참조하는 `AF30`에 표준산소농도 자동 입력
- 기록지 출력본 빈 구분행에 `표준산소 O₂` 및 값/% 표시 (셀 크기/행 높이 변경 없음)
- 시료채취기록지 업체명에 업체관리 데이터 선택목록 연동
- 업체 선택 후 시설명 선택목록을 해당 업체 시설로 자동 제한
- 선택한 업체/시설 ID를 hidden 필드로 저장하여 다음 단계 굴뚝 직경/가로·세로 연동 기반 마련
- 직접입력 기능도 계속 허용
- 기존 Excel 그림 위치, 계산, 업체관리 기능은 변경하지 않음


## v49
- 일정등록 완성: 날짜/구분/담당자/상태/업체/시설/측정항목/일정내용/비고 → 달력 즉시 반영
- 완료 상태 측정출장은 업체 측정이력 자동 반영
- 업체관리 `하반기 측정 미완료만` 필터 추가
- 시설별 측정정보에 굴뚝 단면/내경/가로/세로 입력란 추가
- 시료채취기록지 업체→시설 선택 시 등록된 내경/치수 자동입력
- 기존 DB의 시설 내경 자료는 원형 0건, 사각형 0건으로 현재 미등록 상태
- Excel 기록지(먼지) `유량 (산소보정 후) (Sm3/min)` 값 셀 L25에 correctedFlow 자동 입력
